<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.pucp.analizador">
    <!-- permisos para uso de web services -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- permisos para acceso a la camara -->
    <!-- <uses-permission android:name="android.permission.CAMERA"/> -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- activities -->
        <activity
            android:name=".MainActivity"
            android:icon="@drawable/ic_user_public"
            android:theme="@style/AppTheme.Custom"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ultimas.ultimasActivity"
            android:icon="@drawable/ic_user_public"></activity>
        <activity android:name=".LoginActivity"
            android:windowSoftInputMode="adjustResize"
            android:icon="@drawable/ic_user_public"/>
        <activity
            android:name=".AnalizarActivity"
            android:windowSoftInputMode="adjustResize"
            android:icon="@drawable/ic_user_public"/>
        <activity android:name=".AboutActivity"
            android:icon="@drawable/ic_user_public"
           />

    </application>

</manifest>